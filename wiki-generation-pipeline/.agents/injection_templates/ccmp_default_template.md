<!-- CCMP Standard Injection Template v1 -->
<!-- All injected content blocks carry lthp-highlight by default (LTHP Layer 1). -->

<INJECTED_BLOCK class="lthp-highlight">
  <!-- Injected content goes here. The lthp-highlight class is applied to the
       outermost content-bearing wrapper. On the next edit, run lthp_audit.py
       (LTHP Layer 2) to verify. -->
</INJECTED_BLOCK>

<!-- Usage:
  1. Replace <INJECTED_BLOCK> with the correct content-bearing element (e.g. <div class="card">).
  2. Keep class="lthp-highlight" on the wrapper.
  3. After injection, run: python lthp_audit.py <html_file>
  4. The audit verifies the highlight is present and exits 0 on pass.
-->
