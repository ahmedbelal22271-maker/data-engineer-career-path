# Stage 3: Deep Extraction

Invoke **Phase 2 of the Large File Protocol**. For each chunk, run 4-layer analysis, extract content into `de_wiki/topics/[topic].md` using all extraction rules, update `de_wiki/index.md` incrementally, and log contradictions. Pass the Phase 2 Gate before proceeding.
