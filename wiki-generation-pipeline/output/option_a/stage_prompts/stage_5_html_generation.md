# Stage 5: HTML Generation

Run the Large File Protocol Phase 4 (output map + master summary), then render the wiki into `output/option_a/index.html` as a self-contained wiki-style HTML with inline CSS, collapsible sections, and a table of contents sidebar.
